defmodule Coutarr do
  @moduledoc """
  Documentation for `Coutarr`.
  """

  @doc """
  Hello world.

  ## Examples

      iex> Coutarr.hello()
      :world

  """
  def new(value) do
    value
  end

  def increment(name) do
    "Hello #{name}."
  end

  def decrement(name) do
    "Hello #{name}."
  end

  def show(name) do
    "Hello #{name}."
  end
end
